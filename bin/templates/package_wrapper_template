#!/usr/bin/env bash
# Place copy at: bin/packages/__PKG__/__PKG__
# If your package has internal scripts in bin/packages/__PKG__/bin/*.sh, exec into them here.
set -euo pipefail
source "$(dirname "$0")/../_mapi_conda_lock.sh" 2>/dev/null || true

# Optional: activate a specific env for this package
# conda activate __ENV__ 2>/dev/null || true

# Example: call an internal script
exec "$(dirname "$0")/bin/__ENTRYPOINT__.sh" "$@"

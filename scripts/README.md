scripts that can be run without the use of the MalariAPI format of the "out" folder.
For setup, it will create folders in your HOME directory with common names like "tools". 
        (i.e. ~/tools/blast/...)
Make sure that this will not overwrite anything you wish to keep
